diff --git a/AGENTS.md b/AGENTS.md
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -3,2 +3,2 @@
-> Contexto permanente para agentes de desenvolvimento. Atualizado em 26 de
+> Contexto permanente para agentes de desenvolvimento. Atualizado em 28 de
 > agosto de 2026.
@@ -63,4 +63,8 @@
 - A descricao do negocio melhora a qualidade do perfil, mas e opcional e nao
   pode bloquear a publicacao; especialidade, WhatsApp, cidade, estado e ao
   menos um servico ativo formam os requisitos minimos de descoberta.
+- A publicacao automatica nao depende da agenda. O onboarding profissional,
+  porem, so e considerado concluido depois que o dono salva os horarios pela
+  primeira vez; `agenda_configuracoes.configurado_em` e o marco canonico dessa
+  ativacao. Publicacao e agenda sao etapas distintas do funil.
 - Links antigos de perfis devem continuar funcionando quando o slug mudar.
diff --git a/src/repositories/configuracoesRepository.js b/src/repositories/configuracoesRepository.js
--- a/src/repositories/configuracoesRepository.js
+++ b/src/repositories/configuracoesRepository.js
@@ -45,5 +45,27 @@
   );
 }
 
+async function buscarAgendaConfigurada(
+  usuarioId
+) {
+  const resultado =
+    await db.query(
+      `
+        SELECT
+          configurado_em IS NOT NULL
+            AS configurada
+        FROM agenda_configuracoes
+        WHERE profissional_id = $1
+        LIMIT 1
+      `,
+      [usuarioId]
+    );
+
+  return (
+    resultado.rows[0]
+      ?.configurada === true
+  );
+}
+
 /*
  * No banco, a coluna atual é "whatsapp".
@@ -396,4 +396,5 @@
 module.exports = {
   buscarNegocioDoUsuario,
+  buscarAgendaConfigurada,
   buscarNegocioPorId,
   atualizarNegocio,
diff --git a/src/services/configuracoesService.js b/src/services/configuracoesService.js
--- a/src/services/configuracoesService.js
+++ b/src/services/configuracoesService.js
@@ -627,9 +627,21 @@
     "Negócio não encontrado."
   );
 
-  return montarRespostaNegocio({
-    negocio,
-    papel:
-      vinculo.papel,
-  });
+  const agendaConfigurada =
+    await configuracoesRepository
+      .buscarAgendaConfigurada(
+        usuarioId
+      );
+
+  return {
+    ...montarRespostaNegocio({
+      negocio,
+      papel:
+        vinculo.papel,
+    }),
+    ativacao: {
+      agenda_configurada:
+        agendaConfigurada === true,
+    },
+  };
 }
diff --git a/tests/configuracoes.test.js b/tests/configuracoes.test.js
--- a/tests/configuracoes.test.js
+++ b/tests/configuracoes.test.js
@@ -24,4 +24,7 @@
     buscarNegocioPorId:
       jest.fn(),
 
+    buscarAgendaConfigurada:
+      jest.fn(),
+
     atualizarNegocio:
@@ -215,5 +215,9 @@
     beforeEach(() => {
       jest.clearAllMocks();
+
+      configuracoesRepository
+        .buscarAgendaConfigurada
+        .mockResolvedValue(false);
 
       app =
         criarApp();
@@ -277,4 +277,8 @@
             criarNegocio()
           );
 
+        configuracoesRepository
+          .buscarAgendaConfigurada
+          .mockResolvedValue(true);
+
         const resposta =
@@ -315,4 +315,18 @@
         );
 
         expect(
+          configuracoesRepository
+            .buscarAgendaConfigurada
+        ).toHaveBeenCalledWith(
+          7
+        );
+
+        expect(
+          resposta.body.ativacao
+        ).toEqual({
+          agenda_configurada:
+            true,
+        });
+
+        expect(
           resposta.body.negocio
diff --git a/src/services/agendaConfiguracaoService.js b/src/services/agendaConfiguracaoService.js
--- a/src/services/agendaConfiguracaoService.js
+++ b/src/services/agendaConfiguracaoService.js
@@ -454,4 +454,8 @@
               client
             );
 
+        const primeiraConfiguracao =
+          !configuracaoExistente
+            ?.configurado_em;
+
         let configuracao;
@@ -508,7 +508,20 @@
           );
         }
 
+        const configuracaoMarcada =
+          await agendaConfiguracaoRepository
+            .marcarConfigurada(
+              usuarioId,
+              client
+            );
+
+        configuracao =
+          configuracaoMarcada ||
+          configuracao;
+
         return {
           mensagem:
-            "Horários de atendimento atualizados com sucesso.",
+            primeiraConfiguracao
+              ? "Sua agenda está pronta para receber clientes."
+              : "Horários de atendimento atualizados com sucesso.",
           configuracao,
diff --git a/tests/agenda-configuracao-hardening.test.js b/tests/agenda-configuracao-hardening.test.js
--- a/tests/agenda-configuracao-hardening.test.js
+++ b/tests/agenda-configuracao-hardening.test.js
@@ -12,3 +12,5 @@
     atualizarConfiguracao:
       jest.fn(),
+    marcarConfigurada:
+      jest.fn(),
     listarHorarios:
@@ -105,6 +105,16 @@
         .mockResolvedValue({
           profissional_id: 7,
           duracao_padrao: 60,
+          configurado_em: null,
+        });
+
+      repository
+        .marcarConfigurada
+        .mockResolvedValue({
+          profissional_id: 7,
+          duracao_padrao: 60,
+          configurado_em:
+            "2026-08-28T22:00:00.000Z",
         });
 
       repository
@@ -168,3 +168,34 @@
+        expect(
+          repository
+            .marcarConfigurada
+        ).toHaveBeenCalledWith(
+          7,
+          client
+        );
+
+        expect(
+          repository
+            .marcarConfigurada
+            .mock.invocationCallOrder[0]
+        ).toBeGreaterThan(
+          Math.max(
+            ...repository
+              .salvarHorario
+              .mock.invocationCallOrder
+          )
+        );
+
+        expect(
+          resultado.configuracao
+            .configurado_em
+        ).toBeTruthy();
+
+        expect(
+          resultado.mensagem
+        ).toBe(
+          "Sua agenda está pronta para receber clientes."
+        );
+
         expect(
           resultado.horarios
         ).toHaveLength(7);
@@ -230,5 +230,10 @@
         ).rejects.toThrow(
           "Falha simulada"
         );
+
+        expect(
+          repository
+            .marcarConfigurada
+        ).not.toHaveBeenCalled();
       }
     );
diff --git a/frontend/src/components/ProfessionalOnboardingChecklist.jsx b/frontend/src/components/ProfessionalOnboardingChecklist.jsx
--- a/frontend/src/components/ProfessionalOnboardingChecklist.jsx
+++ b/frontend/src/components/ProfessionalOnboardingChecklist.jsx
@@ -9,7 +9,7 @@
     : [];
 }
 
-export function buildOnboardingSteps({ publication }) {
+export function buildOnboardingSteps({ publication, scheduleConfigured }) {
   const pending = normalizePending(publication);
   const profilePending = pending.filter((item) => item !== SERVICE_PENDING);
 
@@ -43,6 +43,18 @@
       complete: publication?.publicado === true,
       to: "/painel/negocio",
       action: "Verificar publicação"
+    },
+    {
+      id: "agenda",
+      title: "Configure seus horários",
+      description: scheduleConfigured === true
+        ? "Seus horários estão prontos para receber agendamentos."
+        : publication?.publicado
+          ? "Seu perfil está no ar. Agora escolha quando clientes podem agendar."
+          : "Depois da publicação, defina quando clientes podem agendar.",
+      complete: scheduleConfigured === true,
+      to: "/painel/horarios",
+      action: "Configurar horários"
     }
   ];
 }
@@ -50,7 +62,8 @@
 export function ProfessionalOnboardingChecklist({
   businessSlug,
   loading,
-  publication
+  publication,
+  scheduleConfigured
 }) {
   if (!loading && !publication) return null;
@@ -63,7 +76,7 @@
     );
   }
 
-  const steps = buildOnboardingSteps({ publication });
+  const steps = buildOnboardingSteps({ publication, scheduleConfigured });
   const completed = steps.filter((step) => step.complete).length;
   const nextStep = steps.find((step) => !step.complete);
   const progress = Math.round((completed / steps.length) * 100);
@@ -76,13 +89,13 @@
             <ConfirmationIcon className="onboarding-complete-icon" />
             <span>Configuração concluída</span>
           </p>
-          <h2>Seu negócio está pronto para crescer</h2>
+          <h2>Seu negócio está pronto para receber clientes</h2>
           <p className="muted">
-            Compartilhe seu perfil e mantenha serviços e horários atualizados.
+            Sua agenda está configurada. Compartilhe seu perfil e mantenha serviços e horários atualizados.
           </p>
         </div>
         <div className="onboarding-complete-actions">
-          <strong className="onboarding-progress-label">3 de 3</strong>
+          <strong className="onboarding-progress-label">4 de 4</strong>
           {businessSlug && (
             <Link
               className="button"
@@ -105,7 +118,7 @@
             Prepare seu negócio para receber agendamentos
           </h2>
           <p className="muted">
-            Informe os dados essenciais e cadastre um serviço. A publicação é automática e gratuita.
+            Complete o perfil, cadastre um serviço e confirme seus horários. A publicação continua automática e gratuita.
           </p>
         </div>
         <strong className="onboarding-progress-label">
diff --git a/frontend/src/components/ProfessionalOnboardingChecklist.test.jsx b/frontend/src/components/ProfessionalOnboardingChecklist.test.jsx
--- a/frontend/src/components/ProfessionalOnboardingChecklist.test.jsx
+++ b/frontend/src/components/ProfessionalOnboardingChecklist.test.jsx
@@ -18,6 +18,7 @@
           pode_publicar: false,
           pendencias: ["pelo menos um serviço ativo"]
         }}
+        scheduleConfigured={false}
         {...overrides}
       />
     </MemoryRouter>
@@ -28,7 +29,7 @@
   it("leva a profissional para a primeira etapa incompleta", () => {
     renderChecklist();
 
-    expect(screen.getByText("1 de 3")).not.toBeNull();
+    expect(screen.getByText("1 de 4")).not.toBeNull();
     expect(screen.getByText("Nome, contato e localização estão prontos.")).not.toBeNull();
     expect(screen.getByRole("link", { name: "Cadastrar serviço" })
       .getAttribute("href")).toBe("/painel/servicos/novo");
@@ -43,23 +44,40 @@
       }
     });
 
-    expect(screen.getByText("1 de 3")).not.toBeNull();
+    expect(screen.getByText("1 de 4")).not.toBeNull();
     expect(screen.getByRole("link", { name: "Cadastrar serviço" })
       .getAttribute("href")).toBe("/painel/servicos/novo");
   });
 
-  it("celebra a ativação e oferece o perfil público", () => {
+  it("continua o onboarding depois que o perfil é publicado", () => {
     renderChecklist({
       publication: {
         publicado: true,
         pode_publicar: true,
         pendencias: []
-      }
+      },
+      scheduleConfigured: false
     });
 
-    expect(screen.getByText("3 de 3")).not.toBeNull();
+    expect(screen.getByText("3 de 4")).not.toBeNull();
+    expect(screen.getByText(/Seu perfil está no ar/)).not.toBeNull();
+    expect(screen.getByRole("link", { name: "Configurar horários" })
+      .getAttribute("href")).toBe("/painel/horarios");
+  });
+
+  it("celebra a ativação só depois que os horários foram configurados", () => {
+    renderChecklist({
+      publication: {
+        publicado: true,
+        pode_publicar: true,
+        pendencias: []
+      },
+      scheduleConfigured: true
+    });
+
+    expect(screen.getByText("4 de 4")).not.toBeNull();
     expect(screen.getByRole("heading", {
-      name: "Seu negócio está pronto para crescer"
+      name: "Seu negócio está pronto para receber clientes"
     })).not.toBeNull();
     expect(screen.getByRole("link", { name: "Ver meu perfil público" })
       .getAttribute("href")).toBe("/negocio/studio-victor");
diff --git a/frontend/src/pages/DashboardPage.jsx b/frontend/src/pages/DashboardPage.jsx
--- a/frontend/src/pages/DashboardPage.jsx
+++ b/frontend/src/pages/DashboardPage.jsx
@@ -20,2 +20,15 @@
-function buildNextAction({ completedBookings, profileVisits }) {
+function buildNextAction({
+  completedBookings,
+  profileVisits,
+  scheduleConfigured
+}) {
+  if (scheduleConfigured === false) {
+    return {
+      title: "Defina quando clientes podem agendar",
+      description: "Seu perfil pode estar publicado, mas confirme seus horários para liberar disponibilidade real.",
+      primary: { label: "Configurar horários", to: "/painel/horarios" },
+      secondary: { label: "Gerenciar serviços", to: "/painel/servicos" }
+    };
+  }
+
   if (profileVisits >= 5 && completedBookings === 0) {
@@ -43,6 +43,6 @@
   return {
-    title: "Deixe a agenda pronta",
-    description: "Serviços claros e horários atualizados ajudam clientes a confirmar sem voltar ao WhatsApp.",
+    title: "Traga seu primeiro cliente",
+    description: "Sua agenda está pronta. Compartilhe seu perfil e mantenha serviços e horários atualizados.",
     primary: { label: "Abrir agenda", to: "/painel/agenda" },
-    secondary: { label: "Gerenciar serviços", to: "/painel/servicos" }
+    secondary: { label: "Ajustar horários", to: "/painel/horarios" }
   };
@@ -85,5 +85,6 @@
   const [onboarding, setOnboarding] = useState({
     businessSlug: "",
     loading: true,
-    publication: null
+    publication: null,
+    scheduleConfigured: null
   });
@@ -146,5 +146,8 @@
             publicado: business.publicado === true,
             pode_publicar: false,
             pendencias: []
-          }
+          },
+          scheduleConfigured:
+            businessResult.ativacao
+              ?.agenda_configurada === true
         });
@@ -292,2 +292,6 @@
   const bookingLabel = completedBookings === 1 ? "agendamento" : "agendamentos";
-  const nextAction = buildNextAction({ completedBookings, profileVisits });
+  const nextAction = buildNextAction({
+    completedBookings,
+    profileVisits,
+    scheduleConfigured: onboarding.scheduleConfigured
+  });
@@ -328,4 +328,5 @@
         businessSlug={onboarding.businessSlug}
         loading={onboarding.loading}
         publication={onboarding.publication}
+        scheduleConfigured={onboarding.scheduleConfigured}
       />
diff --git a/frontend/src/pages/DashboardPage.test.jsx b/frontend/src/pages/DashboardPage.test.jsx
--- a/frontend/src/pages/DashboardPage.test.jsx
+++ b/frontend/src/pages/DashboardPage.test.jsx
@@ -79,4 +79,5 @@
   publication = {
     negocio: { slug: "studio-aurora", publicado: true },
-    publicacao: { publicado: true, pode_publicar: true, pendencias: [] }
+    publicacao: { publicado: true, pode_publicar: true, pendencias: [] },
+    ativacao: { agenda_configurada: true }
   }
@@ -290,4 +290,45 @@
-    expect(screen.getByText("1 de 3")).not.toBeNull();
+    expect(screen.getByText("1 de 4")).not.toBeNull();
+  });
+
+  it("mantém a ativação aberta até a agenda ser configurada", async () => {
+    mockDashboardRequests({
+      dashboard: {
+        resumo: { agendamentos_periodo: 0, faturamento_periodo: 0, clientes_novos: 0 },
+        performance: {
+          taxa_conversao: 0,
+          visitas_perfil: 0,
+          agendamentos_concluidos: 0,
+          cliques_whatsapp: 0,
+          cliques_maps: 0,
+          favoritos_recebidos: 0
+        },
+        ranking_servicos: []
+      },
+      publication: {
+        negocio: { slug: "studio-aurora", publicado: true },
+        publicacao: {
+          publicado: true,
+          pode_publicar: true,
+          pendencias: []
+        },
+        ativacao: { agenda_configurada: false }
+      }
+    });
+
+    render(<MemoryRouter><DashboardPage /></MemoryRouter>);
+
+    expect(await screen.findByText("3 de 4")).not.toBeNull();
+    expect(screen.getByRole("heading", {
+      name: "Defina quando clientes podem agendar"
+    })).not.toBeNull();
+
+    const scheduleLinks = screen.getAllByRole("link", {
+      name: "Configurar horários"
+    });
+    expect(scheduleLinks.length).toBeGreaterThan(0);
+    expect(scheduleLinks.every((link) =>
+      link.getAttribute("href") === "/painel/horarios"
+    )).toBe(true);
   });
 
   it("destaca a ativação do WhatsApp e registra a autorização em um toque", async () => {
